/**
 * Server-side constants for TrustRail ML service.
 * Server-only — never import this from a client component.
 */
export const ML_PORT = 8001;
export const ML_BASE_URL = `http://localhost:${ML_PORT}`;
